# Ignore common editor and OS files
.vscode/
.DS_Store

# Virtual environments
.venv/
venv/
env/

# Wiki files
WIKI_DRAFT

# Save archive created during work
*.zip
*.tar.gz

# Build artifacts / compiled binaries
# These binaries are created during local builds; use a release/ directory for published artifacts
cpu_throttle
cpu_throttle_ctl
cpu_throttle_tui

# Object files, coverage, and temporary build files
*.o
*.a
*.so
*.dSYM/
*.gcda
*.gcno

# Temporary files
/tmp/
*.log

# Local test / program output files
# These `.out` files are usually build/test output and shouldn't be tracked.
*.out

# Python
__pycache__/
*.py[cod]

# Optional: generated C headers for embedded assets are often committed intentionally.
# If you prefer NOT to commit generated headers, uncomment the next line to ignore them:
# include/*.h
